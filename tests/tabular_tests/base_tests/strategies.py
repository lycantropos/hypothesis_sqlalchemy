from tests.strategies import (data,
                              dialects,
                              max_sizes,
                              metadatas,
                              min_sizes)

data = data
dialects = dialects
min_sizes = min_sizes
max_sizes = max_sizes
metadatas = metadatas
