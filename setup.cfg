[aliases]
test = pytest

[tool:pytest]
addopts = --verbose
          --full-trace
          --capture=no
          --repeat=8
          --cov=hypothesis_sqlalchemy
          --cov-config=.coveragerc
          --junitxml result.xml
